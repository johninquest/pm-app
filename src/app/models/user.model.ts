/* import PocketBase, { Record, Admin } from 'pocketbase';

// Define interfaces for better type safety
interface UserData extends Record {
    email: string;
    emailVisibility: boolean;
    username: string;
    verified: boolean;
    avatar?: string;
    // Add any other custom fields from your users collection
  }
  
  type AuthModel = UserData | Admin | null; */